--- a/pyproject.toml
+++ b/pyproject.toml
@@ -25,7 +25,7 @@
 backoff = ">= 1.0.0, < 3.0.0"
 chardet = ">= 2.0, < 6.0"
 # See https://cryptography.io/en/latest/api-stability/#deprecation
-cryptography = ">= 2.0.0, < 45.0.0"  # pin max to < (major + 3)
+cryptography = ">= 2.0.0, < 47.0.0"  # pin max to < (major + 3)
 pyjwt = ">= 1.5.3, < 3.0.0"
 # requests = ">= 2.2.1, < 3.0.0"
 
--- a/pyproject.rest.toml
+++ b/pyproject.rest.toml
@@ -25,7 +25,7 @@
 backoff = ">= 1.0.0, < 3.0.0"
 chardet = ">= 2.0, < 6.0"
 # See https://cryptography.io/en/latest/api-stability/#deprecation
-cryptography = ">= 2.0.0, < 45.0.0"  # pin max to < (major + 3)
+cryptography = ">= 2.0.0, < 47.0.0"  # pin max to < (major + 3)
 pyjwt = ">= 1.5.3, < 3.0.0"
 requests = ">= 2.2.1, < 3.0.0"
 
